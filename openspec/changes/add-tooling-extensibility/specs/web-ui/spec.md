## ADDED Requirements

### Requirement: Tool selection and config UI
系统 SHALL 提供工具选择与配置界面，支持为每个工具设置参数与环境变量。

#### Scenario: Select and configure tool
- **GIVEN** 用户创建新任务
- **WHEN** 选择工具并配置参数
- **THEN** 该配置应用于本次运行
